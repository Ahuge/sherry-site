{
  "name": "nopatientleftbehind",
  "version": "1.0.0",
  "private": true,
  "description": "No Patient Left Behind — a legacy static website for healthcare accountability frameworks, improvement resources, and award grants. A Grant Innes Legacy Initiative.",
  "scripts": {
    "start": "npx serve .",
    "deploy:pages": "echo 'Deploy to GitHub Pages: push main branch, enable Pages from / (root)'",
    "deploy:netlify": "echo 'Deploy to Netlify: drag ./ folder or connect repo — no build command needed'",
    "deploy:vercel": "echo 'Deploy to Vercel: import project, set output to current directory, no build'"
  },
  "keywords": ["healthcare", "accountability", "static-site", "grant-innes", "nplb"],
  "license": "MIT"
}
